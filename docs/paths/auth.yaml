login:
  post:
    summary: Login endpoint
    description: Takes nothing, returns access and refresh tokens
    operationId: login
    requestBody:
      content:
        application/json:
          schema:
            type: object
            properties: {}
    responses:
      '200':
        description: Login successful
        content:
          application/json:
            schema:
              $ref: '../components/schemas.yaml#/components/schemas/LoginResponse'